# The web app calls the API at same-origin /api (proxied to the server in dev,
# see vite.config.ts). Set this when the API is hosted on another origin — as it
# is for the GitHub Pages build, where CI passes it in from the repo variable.
# VITE_API_ORIGIN=https://replicate-this.uni-muenster.de
